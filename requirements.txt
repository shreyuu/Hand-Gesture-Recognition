# Direct dependencies only. Requires Python 3.9-3.11 (TensorFlow 2.15).
# Note: mediapipe depends on opencv-contrib-python; do NOT also install
# opencv-python — the two packages ship the same cv2 module and conflict.
mediapipe==0.10.21
numpy==1.26.4
opencv-contrib-python==4.11.0.86
tensorflow==2.15.1
gTTS==2.5.4
